{
  "metadata": {
    "name": "cn_{{ extra_app.name | lower}}{{ index }}.{{ extra_app.domain | lower}}",
    "displayName": "{{ extra_app.name | lower}}{{ index }}.{{ extra_app.domain | lower}}",
    "description": "components of Common Name"
  }
}
