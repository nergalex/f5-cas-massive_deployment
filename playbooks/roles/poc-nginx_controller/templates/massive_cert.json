{
  "metadata": {
    "name": "crt_{{extra_app.name | lower}}{{ index }}",
    "displayName": "Certificate {{extra_app.name | lower}} {{ index }}",
    "description": "Generated by CMP"
  },
  "desiredState": {
    "type": "PEM",
    "privateKey": "{%- for line in extra_app.tls.key.split('\r\n') -%}{{ line }}\n{%- endfor -%}",
    "publicCert": "{%- for line in extra_app.tls.crt.split('\r\n') -%}{{ line }}\n{%- endfor -%}"
  }
}