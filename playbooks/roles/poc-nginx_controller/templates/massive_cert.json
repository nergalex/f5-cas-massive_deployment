{
  "metadata": {
    "name": "crt_{{extra_app.name | lower}}{{ index }}",
    "displayName": "Certificate {{extra_app.name | lower}} {{ index }}",
    "description": "Generated by CMP"
  },
  "desiredState": {
    "type": "PEM",
    "privateKey": "{%- for line in stats_key_pair[index].key.split('\n') -%}{%- if line != '' -%}{{ line }}\n{%- endif -%}{%- endfor -%}",
    "publicCert": "{%- for line in stats_key_pair[index].crt.split('\n') -%}{%- if line != '' -%}{{ line }}\n{%- endif -%}{%- endfor -%}"
  }
}