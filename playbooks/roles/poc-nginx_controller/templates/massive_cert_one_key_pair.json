{
  "metadata": {
    "name": "crt_{{extra_app.name | lower}}{{ index }}",
    "displayName": "Certificate {{extra_app.name | lower}} {{ index }}",
    "description": "Generated by CMP",
    "tags": ["{{extra_app.name | lower}}{{ index }}"]
  },
  "desiredState": {
    "type": "PEM",
    "privateKey": "{%- for line in stats_key_pair.key.split('\n') -%}{%- if line != '' -%}{{ line }}\n{%- endif -%}{%- endfor -%}",
    "publicCert": "{%- for line in stats_key_pair.crt.split('\n') -%}{%- if line != '' -%}{{ line }}\n{%- endif -%}{%- endfor -%}"
  }
}