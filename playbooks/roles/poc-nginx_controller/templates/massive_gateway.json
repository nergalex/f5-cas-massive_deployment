{
  "metadata": {
    "name": "gw_{{extra_app.name | lower}}{{ index }}",
    "displayName": "Gateways {{extra_app.name | lower}} {{ index }}",
    "description": "Generated by CMP"
  },
  "desiredState": {
    "ingress": {
      {%- if var_tls_enabled is defined and var_tls_enabled -%}
      "tls": {
        "certRef": {
          "ref": "/services/environments/{{ var_env_prefix }}{{ extra_app.environment | lower }}/certs/crt_{{ extra_app.name | lower }}{{ index }}"
        }
      },
      {%- endif -%}
      "uris": {
      {%- if var_tls_enabled is defined and var_tls_enabled %}
        "https://{{extra_app.name | lower}}{{ index }}.{{extra_app.domain | lower}}{{ index }}": {
      {%- else %}
        "http://{{extra_app.name | lower}}{{ index }}.{{extra_app.domain | lower}}{{ index }}": {
      {%- endif %}
          "matchMethod": "EXACT"
        }
      },
      "placement": {
        "instanceRefs": [
          {%- for gw in var_instance_ids -%}
          {
            "ref": "{{ gw.ref }}"
          }{%- if not loop.last -%},{% endif %}
          {%- endfor -%}
        ]
      }
    }
  }
}
